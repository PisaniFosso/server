// fichier msgsnd.cpp 
#include <errno.h>

struct msgform
{
     long mtype ;
     char mtext[MSG_SIZE_TEXT] ;
     int x;
     int y ; 

 }; // msg structure associée au message 


// envoie le message a la file 
  if(msgsnd(msqid,&msg,(MSG_SIZE_TEXT+8),  IPC_NOWAIT) == -1)

// reception de message
    if(msgrcv(msgid, &msg,(MSG_SIZEMAX+8),  0) != -1)
 



        
